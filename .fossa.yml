version: 3

project:
  locator: SolaceLabs_solace-cloud-cli
  id: SolaceLabs_solace-cloud-cli
  name: solace-cloud-cli
  teams: []
  labels:
    - typescript

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
